# Spark on Docker with using Coretto

## Instructions
- Create the images by running `create-images.sh`.
- Run the cluster from the root of the repo: `docker-compose up --spark-worker=1`
- To do a clean shutdown, `docker-compose down`
